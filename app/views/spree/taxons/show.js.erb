<% if @products.length > 0 %>
  $('#products').append("<%= escape_javascript(render "spree/products/lists" , :products => @products).html_safe %>");
  build_events_add_to_cart();
<% else %>
  $("#products-loading").remove();
<% end %>