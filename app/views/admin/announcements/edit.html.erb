<h1>Editing article</h1>

<%= form_for @article, {:url => main_app.admin_article_path(@article)} do |f| %>
    <%= render 'form', :f => f %>
<% end %>

<%= link_to 'Show', main_app.admin_article_path(@article) %> |
<%= link_to 'Back', main_app.admin_articles_path %>
